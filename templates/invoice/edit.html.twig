{% extends 'base.html.twig' %}

{% block body %}
<div class="page-wrapper">
	<h1 class="page-title">Modification de facture</h1>
	{{ form_start(form) }}
	{{ form_row(form.amount) }}

	<button type="submit">Modifier</button>
	{{ form_end(form) }}

	<a href="{{ path('invoice_manager') }}">back to list</a>
</div>
{% endblock %}
